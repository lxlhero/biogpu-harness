# 镜像配置

registry_prefix: "registry.example.com/biogpu"
base_image: "nvidia/cuda:12.1.0-cudnn8-devel-ubuntu22.04"

# 镜像命名规则
# L1: registry_prefix/<tool_name>:<date>-base
# L2 dev: registry_prefix/<tool_name>:v0.x
# release: registry_prefix/<tool_name>:v1.0

build_platform: "linux/amd64"
