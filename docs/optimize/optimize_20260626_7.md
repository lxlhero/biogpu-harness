# Phase 2.2-B：新增 bio-gpu-trace-analyst-agent

进入 Phase 2.2-B。

本阶段只新增 trace 分析 agent，用于从 tracing logs / evidence logs / task_state / artifacts 中定位多 agent workflow 问题，并输出修复方案。

不要做：

```text id="dzp207"
check_completion_gate.py
hooks
CI
dashboard
rjob_submit.py
真实工具工作区修改
自动修复代码
```

运行 Python 统一使用：

```bash id="pvpdut"
/Users/huron/miniconda3/envs/biogpu-harness/bin/python
```

---

## 1. 新增 agent 文件

新增：

```text id="9x5wo7"
.claude/agents/bio-gpu-trace-analyst-agent.md
```

定位：

```text id="6e1xwe"
bio-gpu-trace-analyst-agent 是多 agent workflow 诊断 agent。

它负责从 trace_context.json、events.jsonl、task_state.json、biogpu_project.yaml、reports/、runs/、baseline/ 中还原执行链，定位 workflow / evidence / state / artifact 问题，并制定修复方案。

它不负责直接修 GPU 代码。
它不负责直接重新运行 benchmark。
它不负责直接修改真实工具源码。
```

---

## 2. 和 problem-analyst-agent 的边界

必须在 agent 文档中写清楚：

```text id="afvwwe"
bio-gpu-problem-analyst-agent：
  负责 GPU 代码失败、精度不达标、Docker/R/Bioconductor/reticulate/runtime 错误等任务执行问题。

bio-gpu-trace-analyst-agent：
  负责 workflow 断链、agent 决策来源不明、task_state 异常、next_action 异常、artifact 缺失、证据链不完整、不同 agent 输出冲突等系统编排问题。
```

一句话：

```text id="fqo1n2"
problem-analyst-agent 分析“任务为什么失败”。
trace-analyst-agent 分析“谁在什么时候把流程带偏了”。
```

---

## 3. Always read

该 agent 启动时必须读取：

```text id="48iq3w"
biogpu_project.yaml
state/task_state.json
state/trace_context.json
logs/events.jsonl
```

如果存在，也读取：

```text id="7n7797"
reports/test_plans/
reports/test_results/
reports/execution_plan.md
reports/final_report.md
```

---

## 4. Read on demand

按需读取：

```text id="wvfxwp"
.claude/knowledge/methodology.md
.claude/knowledge/pitfalls/common.md
skills/bioinformatics-tool-gpu-skills/references/common_failure_modes.md
skills/bioinformatics-tool-gpu-skills/references/gpu-precision-matching.md
skills/bioinformatics-tool-gpu-skills/references/performance_metrics.md
```

不要默认读取所有 skills references。

---

## 5. 主要职责

该 agent 必须能做以下事情：

### 5.1 还原 timeline

根据 `events.jsonl` 按 timestamp 排序，输出时间线：

```text id="dr8g0s"
timestamp
agent
event_type
status
step
trace_id
span_id
artifact_path
next_action
message
```

### 5.2 追踪 trace / span / decision / command / artifact

能够回答：

```text id="nux21r"
哪个 agent 创建了某个 artifact？
哪个 agent 做了某个 decision？
哪个 command 失败了？
哪个 span 触发了当前失败？
哪个 next_action 被谁写入或推进？
precision_config 是哪个 agent 生成的？
```

### 5.3 找 workflow 断链点

重点检查：

```text id="wg0ezp"
1. task_state.next_action 与最近 event.next_action 是否冲突
2. event status=pass 但 artifact_path 不存在
3. test_completed pass 但 precision.json 缺失或 status != pass
4. final_report_written 但 final report 不存在
5. command_executed exit_code != 0
6. decision_made 缺少 decision_id
7. artifact_created 缺少 artifact_path
8. events 缺少 trace_id / span_id / session_id
9. trace_context.json 与 events.jsonl 的 trace_id 不一致
10. agent 声称完成，但没有对应 evidence artifact
```

### 5.4 归因责任 agent

输出最可能需要修复的 agent，例如：

```text id="y7l8zp"
suspect_agent: bio-gpu-test-planner-agent
reason: test_plan was generated but precision_config is missing
```

或：

```text id="koflin"
suspect_agent: bio-gpu-test-runner-agent
reason: test_completed event exists, but precision.json is missing
```

### 5.5 制定修复方案

输出下一步修复计划：

```text id="ei00z0"
1. 应该交给哪个 agent
2. 应该修哪个文件
3. 应该重跑哪个脚本
4. 是否需要回退 task_state.next_action
5. 是否需要重新生成 test_plan
6. 是否需要重新运行 compare_precision.py
```

---

## 6. 输出报告

新增输出目录约定：

```text id="ej71dc"
<workspace>/reports/debug/
```

trace-analyst 每次分析输出：

```text id="qjxjyw"
reports/debug/trace_analysis_<timestamp>.md
```

报告必须包含：

```text id="96qltg"
# Trace Analysis Report

## Summary
- Status:
- Root Cause:
- Suspect Agent:
- Suspect Event:
- Suspect Artifact:

## Trace Context
- task_id:
- session_id:
- trace_id:
- root_span_id:

## Timeline
| Time | Agent | Event Type | Status | Step | Artifact | Next Action |
|---|---|---|---|---|---|---|

## Broken Links
| Type | Evidence | Reason |
|---|---|---|

## Suspect Agent Analysis

## Recommended Fix Plan

## Next Routing
- Recommended next agent:
- Recommended next_action:
```

---

## 7. 事件日志

trace-analyst 自己完成分析后，需要调用：

```bash id="xj448e"
/Users/huron/miniconda3/envs/biogpu-harness/bin/python scripts/log_event.py \
  --workspace <workspace> \
  --agent bio-gpu-trace-analyst-agent \
  --event-type trace_analysis_requested \
  --status pass \
  --step trace_analysis \
  --artifact-path reports/debug/trace_analysis_<timestamp>.md \
  --message "Trace analysis completed"
```

注意：

```text id="osk7p3"
这个 agent 不直接修复，只产出诊断报告和 routing 建议。
```

---

## 8. 更新 harness_config.yaml

如果 `harness_config.yaml` 有 agents 列表，请加入：

```text id="73jvie"
bio-gpu-trace-analyst-agent
```

---

## 9. 更新 workflow 文档

更新：

```text id="gym5br"
docs/architecture/workflow.md
```

新增 trace analyst 介绍：

```text id="kpn9mb"
bio-gpu-trace-analyst-agent：
用于多 agent workflow 的 trace / evidence / state 诊断。
当 validator、gate、agent workflow 出现不一致时，由它读取 trace_context.json、events.jsonl、task_state.json 和 reports/ 来定位断链点、归因责任 agent，并输出修复方案。
```

---

## 10. 新增 Phase 2.2-B 文档

新增：

```text id="qmxkp6"
docs/architecture/phase2_2_trace_analyst_agent.md
```

内容包括：

```text id="y150ey"
1. 为什么需要 trace analyst
2. 和 problem-analyst-agent 的区别
3. 输入文件
4. 输出报告格式
5. 触发场景
6. smoke test 结果
7. 后续 Phase 2.2-C：check_completion_gate.py
```

---

## 11. smoke test

使用 dry-run workspace：

```text id="hxziem"
/Users/huron/code/ai_lab/transfer2gpu/_phase2_dryrun_tool
```

构造一个轻微异常场景，不修改真实工具工作区。

建议异常：

```text id="e0ogv6"
在 dry-run events.jsonl 中追加一个 test_completed pass 事件，
artifact_path 指向 reports/test_results/missing_precision.json，
但该文件不存在。
```

然后调用或模拟 `bio-gpu-trace-analyst-agent` 分析。

要求报告能发现：

```text id="gvd3j6"
1. event status=pass 但 artifact_path 不存在
2. suspect_agent = 产生该 test_completed event 的 agent
3. recommended next agent = bio-gpu-test-runner-agent
4. recommended fix = rerun compare_precision.py or fix artifact_path
```

注意：

```text id="f5k8cf"
如果 Claude Code 当前不能真正“调用 agent”做 smoke test，可以至少创建符合格式的 trace_analysis 报告样例，并记录分析逻辑。
```

---

## 12. 完成后输出

完成后请输出：

```text id="sydbuc"
1. 修改文件列表
2. 新增 agent 文件路径
3. trace analyst 和 problem analyst 的边界
4. trace analyst Always read / Read on demand
5. 输出报告路径示例
6. smoke test 异常场景
7. trace analysis 报告摘要
8. 是否更新 harness_config.yaml
9. 是否更新 workflow.md
10. 是否可以进入 Phase 2.2-C：check_completion_gate.py
11. git commit hash
```
