# Phase 2.1-A：实现 validators

进入 Phase 2.1-A，只做两个校验脚本：

```text
scripts/validate_biogpu_project.py
scripts/validate_task_state.py
```

不要做：

```text
log_event.py
compare_precision.py
hooks
CI
rjob_submit.py
dashboard
真实工具工作区修改
```

## 1. validate_biogpu_project.py

功能：校验工具工作区中的 `biogpu_project.yaml`。

支持两种调用：

```bash
python scripts/validate_biogpu_project.py --workspace /Users/huron/code/ai_lab/transfer2gpu/_phase2_dryrun_tool
python scripts/validate_biogpu_project.py --project /Users/huron/code/ai_lab/transfer2gpu/_phase2_dryrun_tool/biogpu_project.yaml
```

必须校验：

```text
tool_name
mode
harness.root
harness.entrypoint
harness.version
paths.workspace_path
paths.bio_tool_path
paths.state_path
paths.reports_path
paths.runs_path
paths.benchmarks_path
paths.baseline_path
paths.logs_path
paths.artifacts_path
source.status
source.user_specified_source
source.source_url
source.version
source.install_method
user_request.summary
user_request.priority
user_request.speedup_target
user_request.notes
benchmarks.primary_e2e.status
benchmarks.double_check_e2e.status
precision.policy
precision.decided_by
precision.plan_path
```

合法值：

```text
mode: A / B
harness.entrypoint: /bio-gpu-team
source.status: pending / ready / failed / user_provided
source.install_method: auto / git / github / gitcode / pip / conda / local / archive / unknown
precision.policy: auto / planned / approved / fixed
```

路径规则：

```text
workspace_path 不能是 biogpu-harness
workspace_path 不能在 biogpu-harness 下面
reports/runs/baseline/logs/benchmarks/artifacts 必须在 workspace_path 下
state_path 必须在 workspace_path/state/ 下
bio_tool_path 必须在 workspace_path 下
```

输出 JSON：

```json
{
  "status": "pass",
  "file": "...",
  "errors": [],
  "warnings": []
}
```

退出码：

```text
pass: 0
validation fail: 1
script error: 2
```

## 2. validate_task_state.py

功能：校验工具工作区中的 `state/task_state.json`。

支持两种调用：

```bash
python scripts/validate_task_state.py --workspace /Users/huron/code/ai_lab/transfer2gpu/_phase2_dryrun_tool
python scripts/validate_task_state.py --state /Users/huron/code/ai_lab/transfer2gpu/_phase2_dryrun_tool/state/task_state.json
```

必须校验：

```text
task_id
tool_name
mode
current_step
current_role
current_module
module_queue
attempt
max_attempts_per_module
next_action
last_result
human_approval_required
approval_reason
approved
tests.primary_e2e
tests.double_check_e2e
session_request
```

A 模式：

```text
session_request 可以是 null
```

B 模式：

```text
session_request 不能是 null
session_request.tool_name 必须存在
session_request.mode 必须是 B
session_request.request_type 必须合法
session_request.summary 建议非空
```

request_type 合法值：

```text
from_scratch
fix_accuracy
fix_e2e_failure
fix_module_test_failure
fix_runtime_error
fix_docker_or_rjob
optimize_speed
continue_next_module
rerun_benchmark
run_double_check
write_final_report
other
```

next_action 合法值：

```text
initialize_project
run_benchmark
setup_source
build_l1_image
run_profiling
assess_feasibility
plan_primary_e2e
run_primary_cpu_baseline
plan_gpu_implementation
implement_gpu_module
review_gpu_code
run_module_test
diagnose_failure
build_l2_image
run_primary_gpu_compare
ask_double_check
plan_double_check_e2e
run_double_check_e2e
plan_existing_project
write_final_report
done
blocked
manual_review
```

未知 next_action 直接 fail。

输出 JSON，退出码同上：

```text
pass: 0
validation fail: 1
script error: 2
```

## 3. smoke test

使用 dry-run workspace：

```text
/Users/huron/code/ai_lab/transfer2gpu/_phase2_dryrun_tool
```

运行：

```bash
python scripts/validate_biogpu_project.py --workspace /Users/huron/code/ai_lab/transfer2gpu/_phase2_dryrun_tool
python scripts/validate_task_state.py --workspace /Users/huron/code/ai_lab/transfer2gpu/_phase2_dryrun_tool
```

要求都 PASS。

再构造错误样例：

```text
mode 改成 C
或 next_action 改成 invalid_action
```

要求 fail，exit 1。

## 4. 文档

新增：

```text
docs/architecture/phase2_1_validators.md
```

记录：

```text
两个脚本的接口
校验规则
输出格式
smoke test 命令和结果
```

## 5. 完成后输出

完成后请输出：

```text
1. 修改文件列表
2. 两个脚本接口
3. smoke test 命令和结果
4. 错误样例测试结果
5. 是否可以进入 Phase 2.1-B
6. git commit hash
```
